# Environment Variables Template

# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# ============================================
# REQUIRED - Security
# ============================================

# Secret key for JWT token generation
# Generate a strong random string (32+ characters)
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin registration key
# Required when registering the first admin user
# Change this to a secure value
ADMIN_REGISTRATION_KEY=your-admin-registration-key

# ============================================
# OPTIONAL - Server Configuration
# ============================================

# Server port (default: 5000)
PORT=5000

# Node environment (development | production)
NODE_ENV=production

# ============================================
# OPTIONAL - Storage Paths
# ============================================

# These are relative to the server directory
# Defaults are fine for most deployments
# STORAGE_DIR=./storage
# CACHE_DIR=./cache
# DATA_DIR=./data

# ============================================
# OPTIONAL - Upload Limits
# ============================================

# Maximum file upload size (default: 50mb)
# MAX_UPLOAD_SIZE=50mb

# ============================================
# OPTIONAL - Rate Limiting
# ============================================

# Rate limit window in minutes (default: 15)
# RATE_LIMIT_WINDOW=15

# Max requests per window (default: 2000)
# RATE_LIMIT_MAX=2000
